~# Diario de Viagens

Instale:
- Kivy
```
pip install kivy
```
- KivyMD
```
pip install kivymd
```

Para executar as migrations, no terminal execute:
```
foreach ($file in Get-ChildItem *.py) {
  python $file.FullName
}
```
